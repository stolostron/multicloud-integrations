apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: gitops-operator
  name: openshift-gitops-operator-controller-manager-metrics-service
  namespace: {{ .Release.Namespace }}
spec:
  ports:
  - name: https
    port: 8443
    targetPort: 8080
  selector:
    control-plane: gitops-operator
